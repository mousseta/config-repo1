name: Hello World Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  ENV_NAME: production

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Vérifier la variable d'environnement
        run: echo ${{env.ENV_NAME}}

