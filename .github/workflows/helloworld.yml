name: Premier workflow
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build terminé"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Déploiement en cours"
