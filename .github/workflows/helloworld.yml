on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  ENV_NAME: production

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Afficher la variable
        run: echo "Environnement: $ENV_NAME"
